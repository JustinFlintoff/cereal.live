<%= form_with(model: cereal, local: true) do |form| %>
  <% if cereal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cereal.errors.count, "error") %> prohibited this cereal from being saved:</h2>

      <ul>
      <% cereal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
